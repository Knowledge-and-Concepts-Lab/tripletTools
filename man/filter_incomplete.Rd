% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_incomplete.R
\name{filter_incomplete}
\alias{filter_incomplete}
\title{Exclude participants with too few trials}
\usage{
filter_incomplete(d, min_trials = 510)
}
\arguments{
\item{d}{Data frame with a \code{worker_id} column.}

\item{min_trials}{Integer. Minimum number of trials required for a
participant to be retained. Default: \code{510}.}
}
\value{
The input data frame with rows belonging to excluded participants
(and rows with \code{NA} worker IDs) removed.
}
\description{
Removes participants who completed fewer than \code{min_trials} experiment
trials, as well as any rows with a missing \code{worker_id}. This guards
against partially-completed sessions being included in downstream analyses.
}
\examples{
\dontrun{
d <- data.frame(
  worker_id = c(rep("p1", 600), rep("p2", 100)),
  rt        = rnorm(700, 1500, 300)
)
filter_incomplete(d, min_trials = 510)
# only p1 is retained
}

}
