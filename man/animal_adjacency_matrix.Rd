% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animal_adjacency_matrix.R
\docType{data}
\name{animal_adjacency_matrix}
\alias{animal_adjacency_matrix}
\title{Verbal-fluency co-occurrence graph for 295 animal words}
\format{
\subsection{\code{animal_adjacency_matrix}}{

A 295 x 295 numeric matrix with row and column names giving the animal
word at each position. Entry \verb{[i, j]} is the number of times word \code{j}
immediately followed word \code{i} across all participants' lists.
}
}
\source{
Verbal-fluency data from the
\href{https://wls.wisc.edu/}{Wisconsin Longitudinal Study}.
}
\usage{
animal_adjacency_matrix
}
\description{
A directed, weighted adjacency matrix built from verbal-fluency data:
participants listed as many animals as they could think of, and an edge
from word A to word B is weighted by the number of times B immediately
followed A across all lists. Bundled alongside
\code{\link{animal_triplet_embedding}} as example data for estimating
similarity from verbal-fluency data via \code{\link{successor_matrix}}
and \code{\link{hellinger_dist}}, and comparing the resulting distances
to a triplet-based embedding of the same items.
}
\details{
Only words produced by at least 5 participants are included (hence
"min5" in the source file names). This vocabulary is broader than
\code{\link{animal_triplet_embedding}}'s 213 items -- it includes 82
additional words (superordinate terms like "animal", synonyms/variants,
and other species) that appeared in fluency lists but weren't part of
the curated triplet stimulus set. All 213 triplet-embedded items are
present among these 295.

When comparing successor-representation-based distances to
\code{\link{animal_triplet_embedding}}, compute
\code{\link{successor_matrix}} on the full 295-word graph first, and
only restrict to the 213 matching items afterward (subsetting rows and
columns of the resulting successor matrix) -- this preserves genuine
multi-hop associative structure that passes through a non-target word
(e.g. bear -> cub -> lion), which would be lost if the adjacency matrix
were restricted to the 213 items before computing the successor
representation.
}
\keyword{datasets}
